import 'package:flutter/material.dart';

class MobilityManual extends StatelessWidget {
  const MobilityManual({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('차량 사용법');
  }
}
