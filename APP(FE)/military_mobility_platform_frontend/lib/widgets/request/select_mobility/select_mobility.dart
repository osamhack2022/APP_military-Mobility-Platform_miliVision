import 'package:flutter/material.dart';

class SelectMobilityTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('select mobility'));
  }
}
